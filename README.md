## Running server

Start the service:
```bash
./server.sh start
```

Check status:
```bash
./server.sh status
```

Stop the service:
```bash
./server.sh stop
```

Restart:
```bash.
/server.sh restart
```
